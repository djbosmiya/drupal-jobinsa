/**
	* @file
	* Defines Javascript behaviors for the coupon redemption form.
	*/

(function($, Drupal, drupalSettings) {
	'use strict';

	Drupal.behaviors.calculateConsommable = {
		attach: function(context, settings) {
			
		}
	};
})(jQuery, Drupal, drupalSettings);